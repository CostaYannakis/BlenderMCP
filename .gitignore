# Blender autosaves and iterative safety copies
*.blend1
*.blend2
*_pre_*.blend
*_backup_*.blend
balmoral_court*.blend

# Generated verification and comparison renders
/architect_*.png
/entry_*.png

# The game vendors the library builds it needs into game/vendor, so the
# install tree is only required when re-vendoring.
game/node_modules/

# Local/editor noise
__pycache__/
*.py[cod]
.DS_Store
Thumbs.db

